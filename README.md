# trpc-template

Full-Stack TypeScript: tRPC + React + Express + Drizzle ORM + MySQL

## Quick Start

```bash
pnpm install
docker-compose up db -d
pnpm run db:push
pnpm run dev
```

Development: http://localhost:3000
